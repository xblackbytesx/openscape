# Required
DATABASE_URL=postgres://openscape:change_me@openscape-db:5432/openscape?sslmode=disable
SESSION_SECRET=replace-with-at-least-64-random-characters-use-openssl-rand-hex-32

# Optional — shown with defaults
PORT=8080
SECURE_COOKIES=true
ALLOW_REGISTRATION=false
MAX_UPLOAD_MB=100
UPLOADS_PATH=/app/data/uploads
DOCKER_ROOT=/opt/docker
DB_PASSWORD=change_me
